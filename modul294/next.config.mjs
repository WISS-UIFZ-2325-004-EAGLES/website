/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['raw.githubusercontent.com'], // Add this line to allow images from this domain
    },
};

export default nextConfig;
